#23/04/21
#MODULOS PYTHON

#Para importar um modulo de funções inteiros:
#EX: import bebida

#Para importar uma função especifica do modulo:
#EX: from doce import pudim
#Pode ir em "pypi" para ver todas as bibliotecas.

#EXISTEM BIBLIOTECAS PADRÃO
import math #importa o modulo Math com todas as suas funções
from math import sqrt #importa o modulo sqrt de funções matematicas
#quando escrever que função quer, vc pode apertar ctrl + space que aparece todas as funções para escolha.

#Algumas funções:
#ceil --> arredonda um numero para cima.
#floor --> arredonda um numero para baixo.
#
#
#
#
#
#
#sqrt --> calculo de raiz quadrada.

#raiz = math.sqrt(2) #chama a função sqrt do modulo math
#print(sqrt(2)) #

#import random --> modulo random, parqa gerar numeros aleatorios
#num = random.random() --> gera numeros aleatorios entre 0 e 1.
#num = random.randint(1, 10) --> gera um numero aleatórios entre um intervalo escolhido;

#EXISTEM BIBLIOTECAS QUE NÃO VEM INSTALADA NO PC
#Para instalar é só escrecer. EX: import emoji   --> clica em cima da palavra emoji, em seguida na lampada vermelha do lado e então em "install package emoji"
#print(emoji.emojize("Olá, mundo :earth_americas:", use_uliases=True)

